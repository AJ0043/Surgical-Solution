<style>
    .feedback-wrapper {
        display: flex;
        justify-content: center;
        align-items: stretch;
        flex-wrap: wrap;
        max-width: 1100px;
        margin: 50px auto;
        background: rgba(83, 130, 168, 0.6);
        border-radius: 10px;
        overflow: hidden;
        box-shadow: 0 5px 20px rgba(0, 0, 0, 0.1);
    }

    .feedback-image {
        flex: 1 1 400px;
        min-height: 500px;
        overflow: hidden;
    }

    .feedback-image img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        display: block;
    }

    .feedback-container {
        flex: 1 1 500px;
        padding: 30px;
        background: rgba(136, 137, 137, 0.4);
    }

    .feedback-container h2 {
        text-align: center;
        margin-bottom: 25px;
        color: rgb(217, 39, 68);
        font-family: 'Satisfy', cursive;
    }

    .feedback-container label {
        font-weight: bold;
        display: block;
        margin-bottom: 5px;
        color: #444;
    }

    .feedback-container input,
    .feedback-container textarea {
        width: 100%;
        padding: 12px;
        border-radius: 8px;
        border: 1px solid #ccc;
        margin-bottom: 20px;
        font-size: 14px;
    }

    .feedback-container button {
        width: 100%;
        background-color: #05afa1;
        color: white;
        padding: 12px;
        border: none;
        border-radius: 8px;
        font-size: 16px;
        cursor: pointer;
    }

    .feedback-container button:hover {
        background-color: #038f85;
    }

    /* Responsive Design for mobile (465px screen) */
    @media (max-width: 465px) {
        .feedback-wrapper {
            flex-direction: column;
            margin: 20px; /* Adjust margin for smaller screens */
        }

        .feedback-image {
            min-height: 250px; /* Adjust height for smaller screens */
        }

        .feedback-container {
            flex: 1 1 100%; /* Make form take full width on smaller screens */
            padding: 15px; /* Reduce padding on smaller screens */
        }

        .feedback-container h2 {
            font-size: 1.2rem; /* Smaller font size for header on mobile */
        }

        .feedback-container input,
        .feedback-container textarea {
            font-size: 16px; /* Slightly bigger text for easier reading */
        }

        .feedback-container button {
            font-size: 18px; /* Slightly larger button on mobile */
        }
    }

    /* For screens smaller than 768px but larger than 465px */
    @media (max-width: 768px) and (min-width: 466px) {
        .feedback-wrapper {
            flex-direction: row;
        }

        .feedback-image {
            min-height: 350px; /* Adjust for mid-sized screens */
        }

        .feedback-container {
            flex: 1 1 100%;
            padding: 20px;
        }
    }
</style>

<div class="feedback-wrapper">
    <!-- Image section -->
    <div class="feedback-image">
        <img src="{% static 'Images/CO.png' %}" alt="Feedback Side Image">
    </div>

    <!-- Form section -->
    <div class="feedback-container">
        <h2>Contact Form</h2>
        <form method="post" enctype="multipart/form-data">
            {% csrf_token %}

            <!-- Full Name Input -->
            <label for="full_name">Full Name</label>
            <input type="text" id="full_name" name="full_name" placeholder="Your full name..." required>

            <!-- Email Input -->
            <label for="email">Email</label>
            <input type="email" id="email" name="email" placeholder="Your email..." required>

            <!-- Phone Number Input -->
            <label for="phone">Phone Number</label>
            <input type="tel" id="phone" name="phone" placeholder="Your phone number..." required>

            
            <!-- Message Textarea -->
            <label for="message">Message</label>
            <textarea id="message" name="message" rows="5" placeholder="Your message..." required></textarea>

            <!-- Submit Button -->
            <button type="submit">Send Message</button>
        </form>
    </div>
</div>

{% include 'Store/Inc/Footer.html' %}
{% endblock content %}
